# Ephemeral SSH keys / runtime state (kept in HERDR_PLUGIN_STATE_DIR, never in repo)
*.pem
*.key
id_ed25519*
*.pub
state/
.DS_Store

# Subagent-driven-development process scratch (briefs, reports, review diffs)
.superpowers/

# Internal design/planning docs — not shipped publicly
docs/

# Local configuration (copy from .env.example); never commit real accounts
.env

.idea/
